import { Tabs } from "expo-router";


export default function TabBar(){
return (
  <Tabs>
    {/* 
    for eks:
    <Tabs.Screen
        name="index"
        options={{
          title: "Hjem",
           tabBarIcon: ({ color }) => (
            <AntDesign name="home" size={24} color={color} />
          ),
        }}
      />
*/}
  </Tabs>
);
}